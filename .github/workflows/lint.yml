name: Lint & Type Check

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Lint disabled until Ubuntu 26.04 upgrade (ruff, ty, complexipy)."
